import { ConvertDataStructurePipe } from './convert-data-structure.pipe';

describe('ConvertDataStructurePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertDataStructurePipe();
    expect(pipe).toBeTruthy();
  });
});
